package context;

/********is a Java enum used to define a fixed set of keys for storing and retrieving values 
  from  ScenarioContext *******/ 

public enum ContextKeyEnum {
	
	// create all chaining related variable 
	ADMINLOGIN_TOKEN,
	DIETICIAN_USER_ID,
	DIETICIAN_TOKEN,
	PATIENT_ID,
	PATIENT_NAME,
	MORBIDITY_NAME,
	
}
